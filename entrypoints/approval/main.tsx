import React from 'react';
import ReactDOM from 'react-dom/client';
import { ApprovalApp } from './App.js';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ApprovalApp />
  </React.StrictMode>,
);
